import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#A29415",
    },
    yellow: {
      contrastText: '#ffff00',
    },
  },
});

export default theme;
